#' @param f_expr <[`language`]> an expression representing `f`. This is primarly
#' cosmetic; it does not affect what function is called, but may be used for
#' printing purposes and will be available as the first element of [sys.call()]
#' within `f`.
